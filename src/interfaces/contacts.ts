export default interface Contact {
    id: string;
    name: string;
    number: string;
    closenessFactor: number;
    frequency: string;
    relation: string;
    country: string;
    preferableTime: string;
    lastCalled: string;
}